apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "koku-dev.fullname" . }}-test-connection"
  labels:
    {{- include "koku-dev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "koku-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
